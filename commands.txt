git clone https://github.com/SallyShen1234/qtm350-quiz01.git
cd qtm350-quiz01
touch commands.txt
git checkout -b feature-update
mkdir -p reports
touch reports/summary.md
printf "This document provides a summary of the project updates.\n" >> reports/summary.md
git add reports
git commit -m "Add reports directory"
touch reports/file{1..3}.txt
git mv data/raw-data.csv data/input-data.csv
mkdir -p scripts/backup && cp scripts/*.py scripts/backup/
git rm docs/documentation.md
git add -A
git commit -m "Update project files"
touch .gitignore
printf "temp/\ndataset07.csv\n" >> .gitignore
cat .gitignore
git add .gitignore && git commit -m "Add .gitignore file"
git checkout main
git merge feature-update
git add commands.txt
git commit -m "Add commands.txt with full command history"
git push -u origin main
